/**
 * Strong typing or static typing
 *   the variable is declared with type system
 */
//ints
//int is type
//a is variable
//10 is literal(value)
int a = 10
println("a " + a)
a = 20
println("a " + a)

//re initalize with different data type
//strong typed a variable never allows different types which causes compile time error.
a = "hello"
